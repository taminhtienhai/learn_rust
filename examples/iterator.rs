struct BehaviourTree {
    
}

enum HTMLToken {
    Lit(String),
    Open(char),
    Close(char),
    
}

fn main() {
    let sentence = String::from("abc");

    let mut arr = [1,2,3,4,5];

    // let mut iter = arr.iter().enumerate();


    while let Some(it) = arr.iter().next() {

        // let second = iter.nth(2);

        println!("{it}");


        let x = 0;

    }

}